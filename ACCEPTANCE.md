# V1 验收记录

验收日期：2026-09-14（北京时间）。

**结论：代码、真实源站读取和本地页面验收通过；US VPS 部署与域名尚未完成。**

| 项目 | 结果 | 证据与边界 |
|---|---|---|
| 官方协议核对 | 通过 | 核对 AIHOT OpenAPI v1 的 codexResets、Post/Event/Response 定义，与实际响应匹配 |
| 真实 HTTP 同步 | 通过 | 本地服务客户端于 00:30:19 成功读取 40 条事件；源站 checkedAt 为 00:25:42；ETag 和快照写入 SQLite |
| 自动化行为测试 | 25 项通过 | pytest：0 失败，2 条上游测试客户端弃用提醒；pip check 无依赖冲突 |
| 200/304、快照修订/撤回 | 通过 | 304 条件请求及对应时间语义通过模拟响应测试；真实网络本轮验证的是 200，不冒充真实 304 验收 |
| 异常处理 | 通过 | 超时、无效/重复/缺失字段快照、超大响应、空快照、未来/倒退时间、写入失败、限流秒数/HTTP 日期、重启保留等待、故障恢复 |
| 去重与单 Worker | 通过 | 首次历史通知抑制、状态迁移去重、无外发 sender；文件锁拒绝重复 Worker |
| 本地接口 | 通过 | 状态、最新事件、分页筛选、参数校验、健康 200/503、存活端点、安全响应头 |
| 浏览器布局 | 通过 | Chromium 153；1440×1200、390×844、320×740；均无水平溢出与 JS 异常 |
| 浏览器交互 | 通过 | 3 种宽度下，类型筛选、30 条后加载更多，以及本地请求失败保留已有视图 |
| 视觉复核 | 通过 | 已人工查看桌面与 390px 手机截图；截图保留在个人交付包，公开仓库不包含真实原帖截图；验收针对本地页面 |
| Shell 脚本 | 语法通过 | prepare/deploy/verify 通过 bash -n；未在 US 主机执行 |
| Docker 镜像、128 MiB / 0.25 CPU | 待实机验收 | 当前执行环境无 Docker 引擎，未构建/启动真实容器，也未测量资源占用 |
| Portainer Stack | 待部署 | 已交付 compose.yaml 和本地镜像准备脚本，未登录或变更 Portainer |
| Cloudflare DNS / Tunnel / Access | 待接入 | 域名固定 codex.leohub.cc，README 已列准确配置；当前无 Cloudflare 管理会话 |
| Uptime Kuma | 待接入 | 交付内部 URL 和 HTTP 状态码约定，未创建远程监控项 |

## 剩余步骤

1. 在已有 `ssh us` 连接能力的本机工作环境或 US VPS 上打开交付目录。
2. 按 README 构建镜像并在 Portainer 新建 Stack；核对 cloudflared/Kuma 的真实网络模式。
3. 为整个 `codex.leohub.cc` 配置现有邮箱 Access 策略，再添加 Tunnel 路由和 DNS。
4. 新增 Kuma 监控，运行 verify.sh，核对容器资源、数据卷，以及手机授权访问与未授权拦截。

代码实现阶段没有修改远端服务器、Cloudflare 配置或量化系统。本仓库为后续源码交付，部署仍待执行。交付包不含凭据、真实缓存数据库、AIHOT 原始快照、浏览器二进制或 Python 虚拟环境。
